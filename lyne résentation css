/* Corps de la page */
body {
    font-family: 'Arial', sans-serif; /* Police simple et claire */
    background-color: #f4f4f4;
    margin: 0;
    padding: 0;
    line-height: 1.7;
}

/* En-tête */
header {
    background: linear-gradient(to right, #ff7e5f, #feb47b);
    color: white;
    text-align: center;
    padding: 40px 20px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    font-family: 'Arial', sans-serif; /* Retour à la police simple pour l'en-tête */
}

header h1 {
    font-family: 'Arial', sans-serif; /* Police simple et uniforme */
    font-size: 2.5rem; /* Taille réduite pour éviter l'effet écrasé */
    letter-spacing: 1px;
    margin: 0;
}

/* Style pour le menu de navigation dans l'en-tête */
header nav ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
    display: flex; /* Utilisation de flexbox pour un alignement facile */
    justify-content: center; /* Centre les éléments horizontalement */
    align-items: center; /* Centre les éléments verticalement */
    gap: 20px; /* Espacement entre les éléments */
}

header nav ul li {
    display: inline-block;
    margin: 0 15px;
}

header nav ul li a {
    text-decoration: none;
    color: white;
    font-size: 1.1rem;
    transition: color 0.3s ease;
}

/* Hover effect */
header nav ul li a:hover {
    color: #ffcc00;
}

/* Style pour la section avec l'image et le texte */
.content-wrapper {
    display: flex; /* Utilisation de flexbox pour aligner l'image et le texte */
    justify-content: flex-start; /* L'alignement du texte à gauche et de l'image à droite */
    align-items: flex-start; /* Aligne verticalement le texte et l'image en haut */
    gap: 20px; /* Espace entre le texte et l'image */
}

/* Style pour l'image à droite */
.right-image {
    width: 150px; /* Réduction de la taille de l'image */
    height: auto;
    border-radius: 8px; /* Arrondir les coins de l'image */
    object-fit: cover; /* S'assurer que l'image reste bien proportionnée */
    transition: transform 0.3s ease-in-out; /* Animation pour l'effet de zoom */
}

/* Effet de zoom au survol */
.right-image:hover {
    transform: scale(1.1); /* Augmentation de la taille de l'image à 110% */
}

/* Style pour le texte */
.content-wrapper p {
    flex: 1; /* Le texte prend tout l'espace disponible restant */
    font-size: 1.1rem;
    color: #555;
    line-height: 1.8;
    text-align: justify;
}

header nav ul {
    list-style-type: none;
    padding: 0;
    margin-top: 30px;
}

header nav ul li {
    display: inline-block;
    margin: 0 20px;
}

header nav ul li a {
    text-decoration: none;
    color: white;
    font-size: 1.1rem; /* Taille de police réduite pour plus de légèreté */
    font-weight: 500;
    transition: color 0.3s ease, transform 0.3s ease;
}

header nav ul li a:hover {
    color: #ffcc00;
    transform: scale(1.1);
}

/* Sections principales */
main {
    width: 85%;
    margin: 30px auto;
    padding: 20px;
    background-color: white;
    border-radius: 10px;
    box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.05);
}

/* Sections individuelles */
section {
    margin-bottom: 40px;
}

h2 {
    font-size: 1.8rem; /* Taille réduite pour alléger */
    color: #333;
    border-bottom: 3px solid #ff7e5f;
    padding-bottom: 5px;
    margin-bottom: 15px;
}

p, ul {
    font-size: 1.1rem;
    color: #555;
    line-height: 1.8;
    text-align: justify;
}

ul {
    margin-left: 20px;
}

ul li {
    margin-bottom: 10px;
}

.experiences-container {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin-top: 20px;
}

.experiences-container div {
    width: 30%;
    text-align: justify;
    position: relative;
}

.experiences-container div::before {
    font-size: 20px;
    font-weight: bold;
    margin-right: 5px;
    color: #000;
}

.experiences-container div:not(:last-child) {
    border-right: 2px solid #ccc; /* Ajoute un trait vertical entre les paragraphes */
    padding-right: 20px;
}

/* Styles pour les images */
.experiences-container img {
    width: 100%; /* Toutes les images auront la même largeur */
    height: 200px; /* Hauteur uniforme */
    object-fit: cover; /* Assure un bon cadrage sans déformation */
    margin-top: 10px;
    border-radius: 10px;
    transition: transform 0.3s ease, opacity 0.3s ease; /* Animation */
}

/* Effet au survol */
.experiences-container img:hover {
    transform: scale(1.05); /* Agrandit légèrement l'image */
    opacity: 0.8; /* Légère transparence */
}

img {
    max-width: 50%; /* Réduction de toutes les images */
    height: auto;
}

img[src="job.jpg"] {
    max-width: 100%; /* Garde "job.jpg" à sa taille normale */
}

.pilou-clown-container {
    display: flex;
    justify-content: center;
    gap: 10px; /* Espacement entre les images */
}

.pilou-clown-container img {
    width: 45%; /* Ajuste la taille pour qu'elles tiennent côte à côte */
}


/* Effet de surbrillance au survol pour les paragraphes et les listes */
p:hover, li:hover {
    color: #ff7e5f;
    transition: color 0.3s ease;
}

.bali-photo {
    display: flex;
    justify-content: center;
    margin-top: 20px;
}

.bali-photo img {
    width: 50%; /* Réduit la taille de l'image */
    height: auto;
    transition: transform 0.3s ease-in-out;
    border-radius: 10px; /* Arrondi les coins pour un meilleur rendu */
}

.bali-photo img:hover {
    transform: scale(1.1); /* Effet de zoom au survol */
}

.sport-photos {
    display: flex;
    justify-content: center;
    gap: 15px; /* Espacement entre les images */
    margin-top: 20px;
}

.sport-photos img {
    width: 150px; /* Taille uniforme */
    height: 150px;
    object-fit: cover; /* Pour que l'image remplisse bien la zone sans être déformée */
    border-radius: 10px; /* Arrondi des coins */
    transition: transform 0.3s ease; /* Effet de zoom au survol */
}

.sport-photos img:hover {
    transform: scale(1.1); /* Légère agrandissement au survol */
}

/* Footer */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px 0;
    position: relative;
    bottom: 0;
    width: 100%;
    font-size: 1rem;
}

footer hr {
    width: 60%;
    margin: 20px auto;
    border: 1px solid #ff7e5f;
}

/* Responsive : Adaptation mobile */
@media (max-width: 768px) {
    header h1 {
        font-size: 2rem;
    }

    main {
        width: 95%;
    }

    header nav ul li {
        display: block;
        margin: 10px 0;
    }

    header nav ul li a {
        font-size: 1rem;
    }

    footer {
        font-size: 0.9rem;
    }
}
